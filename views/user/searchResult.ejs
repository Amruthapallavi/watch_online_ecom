<!DOCTYPE html>
    <html lang="en">
    <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title>Tick_Track</title>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Shop</title>
 <!-- meta data -->
 <meta charset="utf-8">
 <meta http-equiv="X-UA-Compatible" content="IE=edge">
 <meta name="viewport" content="width=device-width, initial-scale=1">
 <title></title>
 <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
 <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
 <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">

 <!--font-family-->
 <link href="https://fonts.googleapis.com/css?family=Roboto:100,100i,300,300i,400,400i,500,500i,700,700i,900,900i" rel="stylesheet">
 
 <!-- title of site -->
 <title>Furniture</title>

 <!-- For favicon png -->
 <link rel="shortcut icon" type="image/icon" href="/assets/logo/favicon.png"/>

 <!--font-awesome.min.css-->
 <link rel="stylesheet" href="/assets/css/font-awesome.min.css">

 <!--linear icon css-->
 <link rel="stylesheet" href="/assets/css/linearicons.css">

 <!--animate.css-->
 <link rel="stylesheet" href="/assets/css/animate.css">

 <!--owl.carousel.css-->
 <link rel="stylesheet" href="/assets/css/owl.carousel.min.css">
 <link rel="stylesheet" href="/assets/css/owl.theme.default.min.css">
 
 <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&display=swap" rel="stylesheet">

 <!--bootstrap.min.css-->
 <!-- <link rel="stylesheet" href="/assets/css/bootstrap.min.css"> -->
 
 <!-- bootsnav -->
 <link rel="stylesheet" href="/assets/css/bootsnav.css" >	
 
 <!--style.css-->
 <link rel="stylesheet" href="/assets/css/style.css">
 
 <!--responsive.css-->
 <link rel="stylesheet" href="/assets/css/responsive.css">
 <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
 <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
 <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
      <link rel="stylesheet" href="/css/styles.css">
      <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">

  
      <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
      <style>
        .navbar {
          background-color: #000; /* Black background */
        }
        .nav-item {
          color: white; /* White text */
          padding: 10px; /* Space around nav items */
        }
        .nav-item:hover {
          color: #FFD700; /* Gold color on hover */
        }
        .dropdown-menu {
          background-color: #333; /* Darker background for dropdown */
        }
        .dropdown-item {
          color: #fff; /* White text for dropdown items */
        }
        .dropdown-item:hover {
          background-color: #444; /* Slightly lighter on hover */
        }
        .search-form {
          display: flex; /* Use flexbox for alignment */
          justify-content: center; /* Center the form */
          margin: 0 auto; /* Center the form within the navbar */
        }
        .search-bar {
          width: 150px; /* Reduced width */
          border-radius: 20px; /* Rounded corners */
          border: 1px solid #fff; /* White border */
          padding: 8px; /* Inner padding */
          background-color: #222; /* Dark background for input */
          color: white; /* White text for input */
        }
        .search-button {
          border: none; /* Remove border */
          background-color: #f3f2ed; /* Gold background */
          border-radius: 20px; /* Rounded corners */
          color: #000; /* Black text */
          padding: 8px 12px; /* Inner padding */
          margin-left: 5px; /* Space between input and button */
          font-weight: bold; /* Bold text */
        }
        /* Responsive styling */
        @media (max-width: 768px) {
          .search-form {
            width: 100%; /* Full width on smaller screens */
          }
          .search-bar {
            width: auto; /* Auto width for the input */
          }
        }
      </style>
    </head>
<style>
    body {
        font-family: 'Roboto', sans-serif;
        background-color: #f4f4f4;
        color: #333;
    }

    .category-bar {
        display: flex;
        justify-content: center;
        background-color: #070707;
        padding: 10px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
    }

    .category-bar .nav-link {
        color: white;
        margin: 0 20px;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s;
    }

    .category-bar .nav-link:hover {
        color: #ddd;
    }

    select {
        padding: 10px;
        border: 1px solid #ccc;
        background-color: black;
        color: white;
        margin-left: 20px;
        border-radius: 5px;
        appearance: none;
        transition: border-color 0.3s;
    }

    select:hover {
        border-color: #ddd;
    }

    .search-form {
        display: flex;
        justify-content: center;
        margin: 20px 0;
    }

    .search-bar {
        width: 300px;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
        outline: none;
    }

    .search-button {
        background-color: black;
        color: white;
        border: none;
        padding: 10px 15px;
        border-radius: 5px;
        cursor: pointer;
        transition: background-color 0.3s;
    }

    .search-button:hover {
        background-color: #444;
    }

    .section-titles {
        min-width: 100%;
        height: 60px;
        background-color: rgb(236, 234, 234);
        color: black;
        text-align: center;
        padding: 15px 0;
        margin: 20px 0;
        border-radius: 5px;
    }

    .row {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
    }

    .col-md-3 {
        padding: 15px;
    }

    .card {
        background-color: white;
        border: 1px solid #ddd;
        border-radius: 5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        transition: transform 0.3s;
    }

    .card:hover {
        transform: translateY(-5px);
    }

    .fixed-height-card {
        height: 620px;
        display: flex;
        flex-direction: column;
    }

    .card-body {
        flex: 1;
        overflow-y: auto;
        padding: 15px;
    }

    .card-title {
        font-weight: 600;
        font-size: 1.2em;
        margin-bottom: 10px;
    }

    .card-text {
        margin: 5px 0;
    }

    .wishlist-icon {
        color: #f8f3f3;
        border: 1px solid black;
        cursor: pointer;
        font-size: 18px;
        margin-left: 5px;
    }

    .wishlist-icon:hover {
        color: red; /* Change color on hover for visual feedback */
    }

    .pagination {
        display: flex;
        justify-content: center;
        margin: 20px 0;
    }

    .pagination a {
        margin: 0 5px;
        padding: 10px;
        background-color: #ddd;
        text-decoration: none;
        color: black;
        border-radius: 5px;
        transition: background-color 0.3s;
    }

    .pagination a:hover {
        background-color: #ccc;
    }

    .pagination a.active {
        background-color: black;
        color: white;
    }

    .card {
    display: flex;
    flex-direction: column;
    height: 100%;
    background-color: white;
    border: 1px solid #ddd;
    border-radius: 5px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s;
}

.card img {
    max-height: 320px; /* Set a fixed height */
    object-fit: cover; /* Cover the area while maintaining aspect ratio */
    border-top-left-radius: 5px; /* Rounded corners for the top */
    border-top-right-radius: 5px; /* Rounded corners for the top */
}

.card-body {
    flex: 1; /* Allow this section to take up remaining space */
    padding: 15px;
    display: flex;
    flex-direction: column;
    justify-content: space-between; /* Space out the content */
}

.card-title {
    font-weight: 600;
    font-size: 1.2em;
    margin-bottom: 10px;
}

.card-text {
    margin: 5px 0;
    flex-grow: 1; /* Allow text to grow */
    overflow: hidden; /* Hide overflow */
    text-overflow: ellipsis; /* Show ellipsis if text is too long */
    display: -webkit-box; /* Needed for multiline ellipsis */
    -webkit-box-orient: vertical; /* Needed for multiline ellipsis */
   
}

/* Ensure consistent image size and aspect ratio */
.single-new-arrival-bg img {
    width: 100%;          /* Make sure it spans the parent container */
    height: 300px;        /* Set a fixed height for uniformity */
    object-fit: cover;    /* Crop the image to maintain aspect ratio */
    border-radius: 5px;   /* Optional: Add rounded corners */
}

</style>
<style>
    .navbar {
        background-color: #000; /* Black background */
    }
    .nav-item {
        color: white; /* White text */
        padding: 10px; /* Space around nav items */
    }
    .nav-item:hover {
        color: #FFD700; /* Gold color on hover */
    }
    .dropdown-menu {
        background-color: #333; /* Darker background for dropdown */
    }
    .dropdown-item {
        color: #fff; /* White text for dropdown items */
    }
    .dropdown-item:hover {
        background-color: #444; /* Slightly lighter on hover */
    }
    .search-form {
        display: flex; /* Use flexbox for alignment */
        justify-content: center; /* Center the form */
        margin: 0 auto; /* Center the form within the navbar */
    }
    .search-bar {
        width: 150px; /* Reduced width */
        border-radius: 20px; /* Rounded corners */
        border: 1px solid #fff; /* White border */
        padding: 8px; /* Inner padding */
        background-color: #222; /* Dark background for input */
        color: white; /* White text for input */
    }
    .search-button {
        border: none; /* Remove border */
        background-color: #FFD700; /* Gold background */
        border-radius: 20px; /* Rounded corners */
        color: #000; /* Black text */
        padding: 8px 12px; /* Inner padding */
        margin-left: 5px; /* Space between input and button */
        font-weight: bold; /* Bold text */
    }
    .sort-select {
        border: 1px solid #fff; /* White border */
        border-radius: 20px; /* Rounded corners */
        background-color: #222; /* Dark background for select */
        color: white; /* White text for select */
        padding: 8px; /* Inner padding */
        margin-left: 10px; /* Space between search button and select */
    }
    /* Responsive styling */
    @media (max-width: 768px) {
        .search-form {
            width: 100%; /* Full width on smaller screens */
        }
        .search-bar {
            width: auto; /* Auto width for the input */
        }
    }
</style>
<body>
      
    <nav class="navbar navbar-expand-lg navbar-dark bg-black">
  <div class="container-fluid">
    <div class="navbar-left">
      <span class="brand h3">TickTrack</span>
    </div>
    <!-- Toggle button for mobile view -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-center mx-auto">
        <!-- Compact Centered Black and White Search Form -->
        <form class="search-form" action="/search" method="POST"> <!-- Change to GET for search -->
          <input type="text" name="String" class="search-bar" placeholder="Search..." required>
         
          <button type="submit" class="search-button">Go</button>
</form>
<form action="/search" method="GET">
          <select id="category-filter" name="sort">
            <option value="all">Sort by Price</option>
            <option value="low_to_high" <%= sortOption === 'low_to_high' ? 'selected' : '' %>>Low-to-High</option>
            <option value="high_to_low" <%= sortOption === 'high_to_low' ? 'selected' : '' %>>High-to-Low</option>
        </select>
        </form>
      </div>
      <div class="navbar-right">
        <a href="/userHome" class="nav-item">Home</a>
        <a href="/shop" class="nav-item">Shop</a>
        <div class="dropdown">
          <a href="#" class="nav-item dropdown-toggle" role="button" data-bs-toggle="dropdown" aria-expanded="false">My Account</a>
          <ul class="dropdown-menu">
            <li><a href="/user-account" class="dropdown-item">Profile</a></li>
            <li><a href="#settings" class="dropdown-item">Settings</a></li>
          </ul>
        </div>
        <a href="/wishlist" class="nav-item">❤️</a>
        <a href="/cart" class="nav-item">🛒</a>
        <a href="/userLogout" class="nav-item logout" style="background-color: black;">Logout</a>
      </div>
    </div>
  </div>
</nav>

      
    


<section id="top-watches" class="new-arrivals">
    <div class="container mt-3">
        <div class="section-header">
            <h2>Search Result</h2>
        </div><!--/.section-header-->

        <div class="new-arrivals-content">
            <div class="row">
                <% data.forEach(product => { %>
                <div class="col-md-3 col-sm-4">
                    <div class="single-new-arrival">
                        <div class="single-new-arrival-bg">
                            <div id="carousel<%= product._id %>" class="carousel slide" data-bs-ride="false">
                                <div class="carousel-inner">
                                    <% product.image.forEach(function(image, index) { %>
                                    <div class="carousel-item <%= index === 0 ? 'active' : '' %>">
                                        <img src="/<%= image %>" class="d-block w-100" alt="<%= product.pname %>">
                                    </div>
                                    <% }); %>
                                </div>
                                <button class="carousel-control-prev" type="button" data-bs-target="#carousel<%= product._id %>" data-bs-slide="prev">
                                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                    <span class="visually-hidden">Previous</span>
                                </button>
                                <button class="carousel-control-next" type="button" data-bs-target="#carousel<%= product._id %>" data-bs-slide="next">
                                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                    <span class="visually-hidden">Next</span>
                                </button>
                            </div>
                            <div class="new-arrival-cart">
                                <p>
                                    <span class="lnr lnr-cart"></span>
                                    <a href="/add-to-cart/<%= product._id %>">add <span>to </span> cart</a>
                                </p>
                                <p class="arrival-review pull-right">
                                    <a href="/wishlist/add/<%= product._id %>" class="wishlist-icon" onclick="addToWishlist(event, '<%= product.pname %>')">
                                        <i class="fas fa-heart <%= wishlistProductIds.includes(product._id.toString()) ? 'text-danger' : '' %>" title="Add to Wishlist"></i>
                                    </a>
                                    <span class="lnr lnr-frame-expand"></span>
                                </p>
                            </div>
                        </div>
                        <h4><a href="/product/<%= product._id %>"><%= product.pname %></a></h4>
                        <p class="card-text">
                            <% if (product.offerPrice < product.price) { %>
                                <span style="text-decoration: line-through; color: #888;"> ₹ <%= product.price %></span> 
                                <strong style="color: #000;"> ₹ <%= product.offerPrice %></strong>
                                <span class="badge bg-success ms-2">Offer: <%= product.productOffer ? product.productOffer : categoryMap[product.category._id] %>%</span>
                            <% } else { %>
                                <strong style="color: #000;"> ₹ <%= product.price %></strong>
                            <% } %>
                        </p>
                    </div>
                </div>
                <% }) %>
            </div>
        </div><!--/.new-arrivals-content-->
        
        <div class="pagination">
            <% if (currentPage > 1) { %>
                <a href="/search?page=<%= currentPage - 1 %>&sort=<%= sortOption %>">&laquo;</a>
            <% } %>
        
            <% for (let i = 1; i <= totalPages; i++) { %>
                <a href="/search?page=<%= i %>&sort=<%= sortOption %>" class="<%= i === currentPage ? 'active' : '' %>"><%= i %></a>
            <% } %>
        
            <% if (currentPage < totalPages) { %>
                <a href="/search?page=<%= currentPage + 1 %>&sort=<%= sortOption %>">&raquo;</a>
            <% } %>
        </div>
        
    
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
    
    
    
    </div><!--/.container-->
</section>


<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    document.getElementById('category-filter').addEventListener('change', function() {
        const selectedValue = this.value;
        const currentUrl = new URL(window.location.href);
        const page = currentUrl.searchParams.get('page') || 1; // Retain current page
        window.location.href = `${currentUrl.pathname}?page=${page}&sort=${selectedValue}`;
    });
</script>
<script>
  
  
    document.querySelectorAll('wishlist.icon').forEach((button) => {
        button.addEventListener('click', function(event) {
            event.preventDefault(); // Prevent default link behavior
            const productId = this.getAttribute('href').split('/').pop(); // Get product ID from URL

            // Send POST request to add to wishlist
            fetch(`/wishlist/add/${productId}`, { method: 'POST' })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        alert('Successfully added to wishlist!'); // Simple alert
                    } else {
                        alert(data.message); // Show error message if failed
                    }
                })
                .catch(error => {
                    alert('An error occurred while adding to wishlist.');
                    console.error('Error:', error);
                });
        });
    });
</script>

<script src="/assets/js/jquery.js"></script>
        
<!--modernizr.min.js-->
<script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.min.js"></script>

<!--bootstrap.min.js-->
<script src="/assets/js/bootstrap.min.js"></script>

<!-- bootsnav js -->
<script src="/assets/js/bootsnav.js"></script>

<!--owl.carousel.js-->
<script src="/assets/js/owl.carousel.min.js"></script>


<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-easing/1.4.1/jquery.easing.min.js"></script>


<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>


<%- include('../include/userFooter') %>
